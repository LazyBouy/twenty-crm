# Low-priority audit findings backlog — twenty-mcp

Findings that the audit pipeline categorised as `foot-gun` or `cosmetic`. Each entry is **queued** until the sweep threshold fires (count ≥ 5 by default, or manual via `/sweep-lows --force`); at sweep time, the queued entries are bundled into a single GitHub issue and run through the standard `/triage-issues → /implement-issue-fix → /audit-fix → /close-issue` pipeline. Swept entries move to the `Swept (history)` table for institutional memory; they are NOT deleted.

Threshold: `5` queued items. See `.claude/skills/sweep-lows/SKILL.md` for sweep mechanics; see `plans/2026-05-02-low-handling-policy.md` for the policy rationale.

## Queued

| Added | Source audit | Subcategory | One-line description | Suggested resolution |
|---|---|---|---|---|
| 2026-05-12 | [issue-12-...-audit-round-1.md](issue-12-crm-wrapper-name-inference-replace-with-server-metadata-audit-round-1.md) (LOW-2) | cosmetic | implementer's surprise #4 reasoning in issue #12 Implementation notes misdiagnoses the matching algorithm — claims `{myAPIKey, myApiKey}` doesn't ambiguate because `camelToSnakeCase` produces different snake forms, but the actual matcher lowercases ALL forms and compares via `.includes()`, so `{myAPIKey, myApiKey}` WOULD have ambiguated on input `'myapikey'`. The fixture substitution to `{testObject, testobject}` is correct; only the rationale is wrong | annotate the plan's surprises section with a correction note; no code change required |
| 2026-05-12 | [issue-12-...-audit-round-1.md](issue-12-crm-wrapper-name-inference-replace-with-server-metadata-audit-round-1.md) (LOW-3) | foot-gun | the running `twenty-local-mcp-1` Docker container at localhost:4441 is a pre-built `twenty-mcp:local` image that does NOT include the #12/#13 source changes; integration tests bypass it by importing source directly. Risk: a user testing via `curl http://localhost:4441/mcp ...` BEFORE `docker compose build mcp` runs will see the OLD "Tool not found" error and misinterpret the fix as broken | document the proxy-rebuild step (`docker compose build mcp && docker compose up -d mcp`) as part of any "ship to local-stack-via-MCP-endpoint" verification, OR add it to the supervisor's pre-commit checklist for fixes that touch `crm.ts` |
| 2026-05-12 | [issue-12-...-audit-round-1.md](issue-12-crm-wrapper-name-inference-replace-with-server-metadata-audit-round-1.md) (LOW-4) | foot-gun | `crm.ts:18-19` defines `camelToSnakeCase` locally (byte-identical to `twenty-shared/src/utils/strings/camelToSnakeCase.ts` today) because the import-from-twenty-shared path failed at typecheck during the #12 implementation. If `twenty-shared`'s algorithm evolves (e.g., Unicode handling, leading underscore strip) and the local copy doesn't, the L1 bug class the #12 fix was designed to eliminate returns silently | add a drift test that compares the local `camelToSnakeCase` output against `twenty-shared`'s for a curated input list (only when twenty-shared `dist/` is available), OR replace the local copy with the import once `dist/` is reliably built in CI. The auditor noted twenty-shared `dist/` was actually built during this audit via `npx nx lint twenty-mcp` — so the import replacement may already be unblocked |
| 2026-05-12 | [issue-14-...-audit-round-1.md](issue-14-low-sweep-audit-round-1.md) (LOW-3) | foot-gun | `crm.ts` `resolveObjectNames`: after the candidates-list reduction, `candidates[0].nameSingular` and `candidates[0].namePlural` are accessed without optional chaining. TypeScript strict mode allows it because the prior line checks `candidates.length === 1`, but a subtle refactor (e.g. changing to `>= 1`) would re-introduce undefined-access without a type error | change `candidates[0].name*` to `candidates[0]!.name*` (assert) OR convert to `const [only] = candidates; return {nameSingular: only.nameSingular, ...}` after the explicit guard. 1-line edit in a future refactor |
| 2026-05-12 | [issue-14-...-audit-round-1.md](issue-14-low-sweep-audit-round-1.md) (LOW-4) | cosmetic | Plan packages/twenty-mcp/plans/issue-14-low-sweep.md item 4's verification command uses `TWENTY_FRONT_SOURCE=/nonexistent` to verify the throw fires, but the test file computes the path at module load via `join(__dirname, ...)`, not from `process.env` — so the env-var override doesn't work. The implementation is correct; only the plan's verification instructions are misleading | update future similar plan templates to read the env var or use `mv` to rename the file at test setup |
| 2026-05-12 | [issue-14-...-audit-round-1.md](issue-14-low-sweep-audit-round-1.md) (LOW-5) | foot-gun | `parseInnerOrGraphqlArray` test does NOT cover a `null` input (`JSON.stringify(null)`). The throw branch handles it correctly (via the new `describe()` helper post-LOW-1 absorption), but no test asserts it. If a future refactor breaks the `raw === null` branch, no test catches the regression | add `expect(() => parseInnerOrGraphqlArray('null')).toThrow(/unrecognised response shape/);` to parse-metadata-array.test.ts. 3-line test addition |
| 2026-05-12 | [issue-16-17-18-clubbed-audit-round-1.md](issue-16-17-18-clubbed-audit-round-1.md) (LOW-2) | cosmetic | `note-targets.test.ts:89–95` `jest.fn().mockResolvedValue({...})` is a hand-edit, not the output of `prettier --write` on the BEFORE version. Both forms are prettier-clean and idempotent under prettier — the denser form (call chain collapsed; argument broken across lines) was applied manually during the #18 fix-mode cleanup. Identical runtime behaviour; the only impact is that the implementer's claim "the 15 changes are purely mechanical prettier formatting" is slightly imprecise for this one file | leave on disk; no action — both forms prettier-clean and equivalent at any scale |
| 2026-05-12 | [issue-16-17-18-clubbed-audit-round-1.md](issue-16-17-18-clubbed-audit-round-1.md) (LOW-3) | foot-gun | `round-trip.test.ts:354–368` `viewFilterRowSchema.array().parse([])` succeeds vacuously if Twenty returns zero view-filter rows. Plan #17's Zod schema catches "rows with wrong shape" but not "zero rows" — same L2-class risk shifted from one failure mode to another. Today, the test workspace has built-in view filters so this doesn't surface; a fresh-seeded workspace OR a future Twenty change that removes default filters re-introduces the vacuous-pass | add `expect(viewFilterRows.length).toBeGreaterThan(0)` precondition OR seed a known view-filter row in the test fixture; 1–3 line change to round-trip.test.ts |
| 2026-06-27 | [issue-22-...-audit-round-1.md](issue-22-metadata-update-field-select-options-mangling-audit-round-1.md) (LOW-2) | foot-gun | `metadataUpdateFieldInputSchema.options` union array branch marks `value`/`label`/`color` as required strings — stricter than Twenty's freeform inner `options` schema (no item shape, no required sub-fields). Advisory-only today since `server.ts:155` force-casts args and never calls `.parse()`; if SDK arg-validation is ever enabled for this tool, a valid Twenty payload carrying a color-less option would be over-rejected at the boundary | when/if enabling `.parse()` at the metadata tool boundary, loosen the per-option fields to `.optional()` to match Twenty's freeform shape, or use `.passthrough()` semantics |

## Swept (history)

| Swept on | Sweep issue | Items | Plan path | Closed in |
|---|---|---|---|---|
| 2026-05-12 | [#14](https://github.com/LazyBouy/twenty-crm/issues/14) | 10 items spanning 2026-05-02..2026-05-12: foot-guns (findUnresolved/error-msg coupling; views-coverage skip-on-missing; fields-limit-200; views-coverage non-greedy regex; parseInnerOrGraphqlArray silent `[]`; sdk-boundary enableMetadata=true only) and cosmetics (TwentyMcpClient redundant import; prettier drift; loose row typing; coverage-test pattern inconsistency) | (pending — to be filled by triager) | (pending — to be filled by closer) |

## Absorbed pre-commit (history — for traceability of trivial-in-place LOWs that didn't go through the backlog)

For completeness, LOWs that were absorbed pre-commit (so they never appeared in the Queued table) are logged here as a one-line history.

| Date | Source audit | Subcategory | One-line description | Absorbed in commit |
|---|---|---|---|---|
| 2026-05-02 | issue-1-...-audit-round-1.md (LOW 1) | trivial-in-place | broad catch comment understates swallow scope (rewrote in round 2) | 91a42885ef (issue #1 fix) |
| 2026-05-02 | issue-2-...-audit-round-1.md (LOW 1) | trivial-in-place | byte-for-byte warning missing from `metadata_compute_plan_hash` description | 8906f92105 (issue #2 fix) |
| 2026-05-02 | issue-3-...-audit-round-2.md (L-2) | trivial-in-place | integration test asserts wrapper rejected the operand but doesn't independently verify Twenty was never touched (no DB-state check) | (pending — round-2 commit for issue #3 fix) |

## Filed as separate issues (history — for traceability of cross-cutting LOWs)

| Date | Source audit | One-line description | Issue number |
|---|---|---|---|
| 2026-05-02 | issue-2-...-audit-round-1.md (LOW 2) | no test exercises the MCP SDK `tools/list` boundary for any metadata tool (cross-cutting gap shared by all metadata wrappers) | [#6](https://github.com/LazyBouy/twenty-crm/issues/6) |
